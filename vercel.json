{
  "buildCommand": "pnpm --filter @workspace/ultrofix run build",
  "outputDirectory": "artifacts/ultrofix/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
